/*
     here in this example you cannot directly use the methods and variables of the module.
     If this were so then there could be ambiguity if two modules had the same package name
     The correct version of this program is in next file
*/
require('./greet.js');
greet();