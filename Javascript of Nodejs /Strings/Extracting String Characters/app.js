var val = "Hello world";

// charAt() returns the character at the given location
console.log(val.charAt(0));

// charCodeAt() returns the unicode character value at the given location
console.log(val.charCodeAt(0));