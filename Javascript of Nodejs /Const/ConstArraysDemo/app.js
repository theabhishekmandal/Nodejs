// You can create const arrays and change them but, you cannot assign a new array to the variable

const arr = [1, 2, 3, 4];

// changing a value at an array position
arr[0] = "changing value";
console.log(arr);

// arr = [] <- this will throw an error