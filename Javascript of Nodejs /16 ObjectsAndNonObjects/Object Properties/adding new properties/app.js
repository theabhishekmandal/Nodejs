// adding new properties to the objects
var person = {
    firstname : 'Abhishek',
    lastname : 'Mandal',
};

console.log(person);

// adding new properties
person.age = 21;
person.gender = 'Male';

console.log(person);